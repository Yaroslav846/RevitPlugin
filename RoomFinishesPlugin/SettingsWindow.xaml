<Window x:Class="RevitPlugin.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Настройки" Height="320" Width="480"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown">

    <Window.Resources>
        <SolidColorBrush x:Key="MainBg" Color="#1E1E1E"/>
        <SolidColorBrush x:Key="InputBg" Color="#2D2D30"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#3E3E42"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F1F1F1"/>
        <SolidColorBrush x:Key="AccentColor" Color="#007ACC"/>
        <SolidColorBrush x:Key="AccentHover" Color="#1C97EA"/>

        <!-- Стиль TextBox -->
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource InputBg}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentColor}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#555"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Стиль кнопок -->
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="15,6"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Основная рамка с тенью (опционально) и границей -->
    <Border Background="{StaticResource MainBg}" BorderBrush="{StaticResource AccentColor}" BorderThickness="1" CornerRadius="0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/> <!-- Заголовок -->
                <RowDefinition Height="*"/>  <!-- Контент -->
                <RowDefinition Height="60"/> <!-- Кнопки -->
            </Grid.RowDefinitions>

            <!-- Заголовок -->
            <Grid Grid.Row="0" Background="{StaticResource InputBg}">
                <TextBlock Text="Настройки параметров" Foreground="{StaticResource TextBrush}" 
                           VerticalAlignment="Center" Margin="15,0" FontWeight="SemiBold" FontSize="14"/>
                <Button Content="✕" HorizontalAlignment="Right" Click="btnCancel_Click" 
                        Width="40" Background="Transparent" BorderThickness="0" Foreground="#999"/>
            </Grid>

            <!-- Контент -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="15"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Площадь стен:" Foreground="#AAA" VerticalAlignment="Center"/>
                <TextBox x:Name="txtWall" Grid.Column="1" Margin="15,0,0,0"/>

                <TextBlock Text="Плинтус:" Grid.Row="2" Foreground="#AAA" VerticalAlignment="Center"/>
                <TextBox x:Name="txtSkirt" Grid.Row="2" Grid.Column="1" Margin="15,0,0,0"/>

                <TextBlock Text="Высота:" Grid.Row="4" Foreground="#AAA" VerticalAlignment="Center"/>
                <TextBox x:Name="txtHeight" Grid.Row="4" Grid.Column="1" Margin="15,0,0,0"/>
                
                <!-- Подсказка -->
                <Border Grid.Row="6" Grid.ColumnSpan="2" Background="#2D2D30" Padding="10" CornerRadius="3" BorderBrush="#3E3E42" BorderThickness="1">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="ℹ" Foreground="{StaticResource AccentColor}" FontWeight="Bold" Margin="0,0,8,0"/>
                        <TextBlock Text="Введите точные имена параметров из Revit." Foreground="Gray" FontStyle="Italic" FontSize="11"/>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Кнопки -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,20">
                <Button Content="Отмена" Click="btnCancel_Click" Background="Transparent" Foreground="#AAA" Margin="0,0,10,0"/>
                <Button Content="Сохранить" Click="btnSave_Click" Background="{StaticResource AccentColor}" FontWeight="SemiBold" Padding="20,8"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>